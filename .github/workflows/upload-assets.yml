name: Upload release assets

on:
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  
jobs:
  upload-assets:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Replace URLs in OnePower.js except lines with /assets/
        run: |
          awk '
            /\/assets\// {
              # Om raden innehåller "/assets/", hoppa över ändringar
              print
              next
            }
            {
              # Ersätt URL
              gsub(/https:\/\/raw\.githubusercontent\.com\/flopp999\/Scriptable-OnePower\/main\//,
                   "https://github.com/flopp999/Scriptable-OnePower/releases/latest/download/")
              # Ersätt FileManager
              gsub(/FileManager\.iCloud\(\)/, "FileManager.local()")
              print
            }
          ' OnePower.js > tmp && mv tmp OnePower.js

      - name: Upload assets to release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            OnePower.js
            Translations.json
            Version.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
